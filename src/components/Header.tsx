import React from 'react';

const HEADER_TEXT = 'Moonshot Calendar Inc.';

const Header = () => {
    return (
        <header>_
            <h1>{HEADER_TEXT}</h1>
        </header>
    );
};

export default Header;